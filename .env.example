# Base URL for the backend API. Must include the /api suffix.
# Set this in the Vercel project's Environment Variables (all environments).
# Example for the deployed Flask backend:
VITE_API_URL=https://nutsoul-be.vercel.app/api
